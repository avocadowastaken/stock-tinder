{
  "extends": ["@dc0de/eslint-config-react-native"],
  "rules": {
    //
    // eslint
    //

    "no-restricted-syntax": [
      "error",
      {
        "selector": "ForInStatement",
        "message":
          "for..in loops iterate over the entire prototype chain, which is virtually never what you want. Use Object.{keys,values,entries}, and iterate over the resulting array."
      },

      {
        "selector": "ForOfStatement",
        "message":
          "iterators/generators require regenerator-runtime, which is too heavyweight."
      },

      {
        "selector": "AwaitExpression",
        "message":
          "async/await declarations get transformed to iterators/generators that require regenerator-runtime, which is too heavyweight."
      },

      {
        "selector": "LabeledStatement",
        "message":
          "Labels are a form of GOTO; using them makes code confusing and hard to maintain and understand."
      },

      {
        "selector": "WithStatement",
        "message":
          "`with` is disallowed in strict mode because it makes code impossible to predict and optimize."
      }
    ],
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "lodash-es",
            "message": "Use lodash-es."
          },

          {
            "name": "lodash-es",
            "importNames": ["default"],
            "message": "Do not use default import from lodash-es."
          }
        ]
      }
    ],

    "import/no-internal-modules": [
      "error",
      {
        "allow": ["**/*.png", "**/*.jpg", "rxjs/operators"]
      }
    ],

    "jsx-a11y/accessible-emoji": ["off"]
  }
}
